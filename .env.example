# Required
OPENAI_API_KEY=your_openai_api_key_here

# Optional: only needed if you want email delivery
RESEND_API_KEY=
SENDER_EMAIL=

# Optional: override the default Analyst model
ANALYST_MODEL=

# Optional: fallback recipient used when run_delivery() is called
# without an explicit recipient.
RECIPIENT_EMAIL=
